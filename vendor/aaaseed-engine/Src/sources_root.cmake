# Generated by tools/extract_sources_from_vcxproj.ps1 (CMake migration, Phase C).
# Edit by hand only after disabling regeneration -- this file is meant to be authoritative.

target_sources(AAASeed PRIVATE
    ${CMAKE_SOURCE_DIR}/Src/aaaseed.cpp
    ${CMAKE_SOURCE_DIR}/Src/action.cpp
    ${CMAKE_SOURCE_DIR}/Src/aaa_check_cpu.cpp
    ${CMAKE_SOURCE_DIR}/Src/aaa_build_config.h
    ${CMAKE_SOURCE_DIR}/Src/aaa_check_cpu.h
    ${CMAKE_SOURCE_DIR}/Src/aaa_os.h
    ${CMAKE_SOURCE_DIR}/Src/aaaseed.h
    ${CMAKE_SOURCE_DIR}/src/action.h
)


# aaa_check_cpu.cpp must NOT use AVX2 itself : it runs BEFORE any other code on
# Metal/Release/Debug builds (which are /arch:AVX2) to detect missing AVX2 and show
# a MessageBox pointing the user to AAASeed_Wood.exe. Override the target's /arch:AVX2
# with /arch:SSE2 for this single TU. The /arch flag obeys "last wins" so we don't have
# to strip the prior value.
set_source_files_properties(${CMAKE_SOURCE_DIR}/Src/aaa_check_cpu.cpp PROPERTIES
    COMPILE_OPTIONS "/arch:SSE2"
)
